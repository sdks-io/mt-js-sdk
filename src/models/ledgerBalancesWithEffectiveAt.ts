/**
 * ModernTreasury
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, nullable, object, Schema, string } from '../schema.js';
import { LedgerBalance, ledgerBalanceSchema } from './ledgerBalance.js';

export interface LedgerBalancesWithEffectiveAt {
  /** The inclusive lower bound of the effective_at timestamp for the returned balances. */
  effectiveAtLowerBound: string | null;
  /** The exclusive upper bound of the effective_at timestamp for the returned balances. */
  effectiveAtUpperBound: string | null;
  pendingBalance: LedgerBalance;
  postedBalance: LedgerBalance;
  availableBalance: LedgerBalance;
}

export const ledgerBalancesWithEffectiveAtSchema: Schema<LedgerBalancesWithEffectiveAt> = lazy(
  () =>
    object({
      effectiveAtLowerBound: ['effective_at_lower_bound', nullable(string())],
      effectiveAtUpperBound: ['effective_at_upper_bound', nullable(string())],
      pendingBalance: ['pending_balance', ledgerBalanceSchema],
      postedBalance: ['posted_balance', ledgerBalanceSchema],
      availableBalance: ['available_balance', ledgerBalanceSchema],
    })
);
