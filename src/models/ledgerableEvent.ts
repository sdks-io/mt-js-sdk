/**
 * Modern TreasuryLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  boolean,
  dict,
  nullable,
  number,
  object,
  optional,
  Schema,
  string,
  unknown,
} from '../schema.js';

export interface LedgerableEvent {
  id: string;
  object: string;
  /** This field will be true if this object exists in the live environment or false if it exists in the test environment. */
  liveMode: boolean;
  createdAt: string;
  updatedAt: string;
  /** Name of the ledgerable event. */
  name: string;
  /** Description of the ledgerable event. */
  description: string | null;
  /** One of `credit`, `debit`. */
  direction: string | null;
  /** Value in specified currency's smallest unit. e.g. $10 would be represented as 1000. */
  amount: number;
  /** An ISO 4217 conformed currency or a custom currency. */
  currency: string;
  /** Must be included if currency is a custom currency. The currency_exponent cannot exceed 30. */
  currencyExponent: number | null;
  /** Additionally data to be used by the Ledger Event Handler. */
  customData?: unknown | null;
  /** Id of the ledger event handler that is used to create a ledger transaction. */
  ledgerEventHandlerId: string;
  /** Additional data represented as key-value pairs. Both the key and value must be strings. */
  metadata: Record<string, string> | null;
}

export const ledgerableEventSchema: Schema<LedgerableEvent> = object({
  id: ['id', string()],
  object: ['object', string()],
  liveMode: ['live_mode', boolean()],
  createdAt: ['created_at', string()],
  updatedAt: ['updated_at', string()],
  name: ['name', string()],
  description: ['description', nullable(string())],
  direction: ['direction', nullable(string())],
  amount: ['amount', number()],
  currency: ['currency', string()],
  currencyExponent: ['currency_exponent', nullable(number())],
  customData: ['custom_data', optional(nullable(unknown()))],
  ledgerEventHandlerId: ['ledger_event_handler_id', string()],
  metadata: ['metadata', nullable(dict(string()))],
});
