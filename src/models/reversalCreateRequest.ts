/**
 * Modern TreasuryLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { dict, lazy, object, optional, Schema, string } from '../schema.js';
import {
  LedgerTransactionCreateRequest,
  ledgerTransactionCreateRequestSchema,
} from './ledgerTransactionCreateRequest.js';
import { Reason1Enum, reason1EnumSchema } from './reason1Enum.js';

export interface ReversalCreateRequest {
  /** The reason for the reversal. Must be one of `duplicate`, `incorrect_amount`, `incorrect_receiving_account`, `date_earlier_than_intended`, `date_later_than_intended`. */
  reason: Reason1Enum;
  /** Additional data represented as key-value pairs. Both the key and value must be strings. */
  metadata?: Record<string, string>;
  ledgerTransaction?: LedgerTransactionCreateRequest;
}

export const reversalCreateRequestSchema: Schema<ReversalCreateRequest> = lazy(
  () =>
    object({
      reason: ['reason', reason1EnumSchema],
      metadata: ['metadata', optional(dict(string()))],
      ledgerTransaction: [
        'ledger_transaction',
        optional(ledgerTransactionCreateRequestSchema),
      ],
    })
);
