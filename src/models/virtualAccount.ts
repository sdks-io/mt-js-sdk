/**
 * Modern TreasuryLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  dict,
  lazy,
  nullable,
  object,
  Schema,
  string,
} from '../schema.js';
import { AccountDetail, accountDetailSchema } from './accountDetail.js';
import { RoutingDetail, routingDetailSchema } from './routingDetail.js';

export interface VirtualAccount {
  id: string;
  object: string;
  /** This field will be true if this object exists in the live environment or false if it exists in the test environment. */
  liveMode: boolean;
  createdAt: string;
  updatedAt: string;
  discardedAt: string | null;
  /** The name of the virtual account. */
  name: string;
  /** An optional free-form description for internal use. */
  description: string | null;
  /** The ID of a counterparty that the virtual account belongs to. Optional. */
  counterpartyId: string | null;
  /** The ID of the internal account that the virtual account is in. */
  internalAccountId: string;
  /** An array of account detail objects. */
  accountDetails: AccountDetail[];
  /** An array of routing detail objects. These will be the routing details of the internal account. */
  routingDetails: RoutingDetail[];
  /** The ID of a debit normal ledger account. When money enters the virtual account, this ledger account will be debited. Must be accompanied by a credit_ledger_account_id if present. */
  debitLedgerAccountId: string | null;
  /** The ID of a credit normal ledger account. When money enters the virtual account, this ledger account will be credited. Must be accompanied by a debit_ledger_account_id if present. */
  creditLedgerAccountId: string | null;
  /** Additional data represented as key-value pairs. Both the key and value must be strings. */
  metadata: Record<string, string>;
}

export const virtualAccountSchema: Schema<VirtualAccount> = lazy(() =>
  object({
    id: ['id', string()],
    object: ['object', string()],
    liveMode: ['live_mode', boolean()],
    createdAt: ['created_at', string()],
    updatedAt: ['updated_at', string()],
    discardedAt: ['discarded_at', nullable(string())],
    name: ['name', string()],
    description: ['description', nullable(string())],
    counterpartyId: ['counterparty_id', nullable(string())],
    internalAccountId: ['internal_account_id', string()],
    accountDetails: ['account_details', array(accountDetailSchema)],
    routingDetails: ['routing_details', array(routingDetailSchema)],
    debitLedgerAccountId: ['debit_ledger_account_id', nullable(string())],
    creditLedgerAccountId: ['credit_ledger_account_id', nullable(string())],
    metadata: ['metadata', dict(string())],
  })
);
