/**
 * Modern TreasuryLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  boolean,
  nullable,
  number,
  object,
  optional,
  Schema,
  string,
} from '../schema.js';
import { Direction9, direction9Schema } from './direction9.js';
import { Status17, status17Schema } from './status17.js';

export interface PaymentFlow {
  id?: string;
  object?: string;
  /** This field will be true if this object exists in the live environment or false if it exists in the test environment. */
  liveMode?: boolean;
  createdAt?: string;
  updatedAt?: string;
  /** The client token of the payment flow. This token can be used to embed a payment workflow in your client-side application. */
  clientToken?: string;
  /** The current status of the payment flow. One of `pending`, `completed`, `expired`, or `cancelled`. */
  status?: Status17;
  /** Value in specified currency's smallest unit. e.g. $10 would be represented as 1000. Can be any integer up to 36 digits. */
  amount?: number;
  /** The currency of the payment. */
  currency?: string;
  /** Describes the direction money is flowing in the transaction. Can only be `debit`. A `debit` pulls money from someone else's account to your own. */
  direction?: Direction9;
  /** The ID of a counterparty associated with the payment. As part of the payment workflow an external account will be associated with this counterparty. */
  counterpartyId?: string | null;
  /** If present, the ID of the external account created using this flow. */
  receivingAccountId?: string | null;
  /** The ID of one of your organization's internal accounts. */
  originatingAccountId?: string | null;
  /** If present, the ID of the payment order created using this flow. */
  paymentOrderId?: string | null;
}

export const paymentFlowSchema: Schema<PaymentFlow> = object({
  id: ['id', optional(string())],
  object: ['object', optional(string())],
  liveMode: ['live_mode', optional(boolean())],
  createdAt: ['created_at', optional(string())],
  updatedAt: ['updated_at', optional(string())],
  clientToken: ['client_token', optional(string())],
  status: ['status', optional(status17Schema)],
  amount: ['amount', optional(number())],
  currency: ['currency', optional(string())],
  direction: ['direction', optional(direction9Schema)],
  counterpartyId: ['counterparty_id', optional(nullable(string()))],
  receivingAccountId: ['receiving_account_id', optional(nullable(string()))],
  originatingAccountId: [
    'originating_account_id',
    optional(nullable(string())),
  ],
  paymentOrderId: ['payment_order_id', optional(nullable(string()))],
});
