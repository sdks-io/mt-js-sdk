/**
 * Modern TreasuryLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import { Connection, connectionSchema } from '../models/connection.js';
import { array, nullable, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';

export class ConnectionApi extends BaseApi {
  /**
   * Get a list of all connections.
   *
   * @param afterCursor
   * @param perPage
   * @param vendorCustomerId   An identifier assigned by the vendor to your organization.
   * @param entity             A string code representing the vendor (i.e. bank).
   * @return Response from the API call
   */
  async listConnections(
    afterCursor?: string | null,
    perPage?: number,
    vendorCustomerId?: string,
    entity?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Connection[]>> {
    const req = this.createRequest('GET', '/api/connections');
    const mapped = req.prepareArgs({
      afterCursor: [afterCursor, optional(nullable(string()))],
      perPage: [perPage, optional(number())],
      vendorCustomerId: [vendorCustomerId, optional(string())],
      entity: [entity, optional(string())],
    });
    req.query('after_cursor', mapped.afterCursor);
    req.query('per_page', mapped.perPage);
    req.query('vendor_customer_id', mapped.vendorCustomerId);
    req.query('entity', mapped.entity);
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(array(connectionSchema), requestOptions);
  }
}
