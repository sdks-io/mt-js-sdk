/**
 * Modern TreasuryLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  Direction15Enum,
  direction15EnumSchema,
} from '../models/direction15Enum.js';
import {
  ExpectedPayment,
  expectedPaymentSchema,
} from '../models/expectedPayment.js';
import {
  ExpectedPaymentCreateRequest,
  expectedPaymentCreateRequestSchema,
} from '../models/expectedPaymentCreateRequest.js';
import {
  ExpectedPaymentUpdateRequest,
  expectedPaymentUpdateRequestSchema,
} from '../models/expectedPaymentUpdateRequest.js';
import { Status20Enum, status20EnumSchema } from '../models/status20Enum.js';
import { Type13Enum, type13EnumSchema } from '../models/type13Enum.js';
import { array, dict, nullable, number, optional, string } from '../schema.js';
import { BaseController } from './baseController.js';
import { ErrorMessageError } from '../errors/errorMessageError.js';

export class ExpectedPaymentController extends BaseController {
  /**
   * @param afterCursor
   * @param perPage
   * @param status                 One of unreconciled, reconciled, or archived.
   * @param internalAccountId      Specify internal_account_id to see expected_payments for
   *                                                         a specific account.
   * @param direction              One of credit, debit
   * @param type                   One of: ach, au_becs, bacs, book, check, eft, interac,
   *                                                         provxchange, rtp,sen, sepa, signet, wire
   * @param counterpartyId         Specify counterparty_id to see expected_payments for a
   *                                                         specific account.
   * @param metadata               For example, if you want to query for records with
   *                                                         metadata key `Type` and value `Loan`, the query would be
   *                                                         `metadata%5BType%5D=Loan`. This encodes the query
   *                                                         parameters.
   * @param createdAtLowerBound    Used to return expected payments created after some
   *                                                         datetime
   * @param createdAtUpperBound    Used to return expected payments created before some
   *                                                         datetime
   * @return Response from the API call
   */
  async listExpectedPayments(
    afterCursor?: string | null,
    perPage?: number,
    status?: Status20Enum,
    internalAccountId?: string,
    direction?: Direction15Enum,
    type?: Type13Enum,
    counterpartyId?: string,
    metadata?: Record<string, string>,
    createdAtLowerBound?: string,
    createdAtUpperBound?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExpectedPayment[]>> {
    const req = this.createRequest('GET', '/api/expected_payments');
    const mapped = req.prepareArgs({
      afterCursor: [afterCursor, optional(nullable(string()))],
      perPage: [perPage, optional(number())],
      status: [status, optional(status20EnumSchema)],
      internalAccountId: [internalAccountId, optional(string())],
      direction: [direction, optional(direction15EnumSchema)],
      type: [type, optional(type13EnumSchema)],
      counterpartyId: [counterpartyId, optional(string())],
      metadata: [metadata, optional(dict(string()))],
      createdAtLowerBound: [createdAtLowerBound, optional(string())],
      createdAtUpperBound: [createdAtUpperBound, optional(string())],
    });
    req.query('after_cursor', mapped.afterCursor);
    req.query('per_page', mapped.perPage);
    req.query('status', mapped.status);
    req.query('internal_account_id', mapped.internalAccountId);
    req.query('direction', mapped.direction);
    req.query('type', mapped.type);
    req.query('counterparty_id', mapped.counterpartyId);
    req.query('metadata', mapped.metadata);
    req.query('created_at_lower_bound', mapped.createdAtLowerBound);
    req.query('created_at_upper_bound', mapped.createdAtUpperBound);
    req.throwOn(401, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(array(expectedPaymentSchema), requestOptions);
  }

  /**
   * @param idempotencyKey  This key should be something unique, preferably
   *                                                               something like an UUID.
   * @param body
   * @return Response from the API call
   */
  async createExpectedPayment(
    idempotencyKey?: string,
    body?: ExpectedPaymentCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExpectedPayment>> {
    const req = this.createRequest('POST', '/api/expected_payments');
    const mapped = req.prepareArgs({
      idempotencyKey: [idempotencyKey, optional(string())],
      body: [body, optional(expectedPaymentCreateRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.header('Idempotency-Key', mapped.idempotencyKey);
    req.json(mapped.body);
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(expectedPaymentSchema, requestOptions);
  }

  /**
   * @param id id
   * @return Response from the API call
   */
  async getExpectedPayment(
    id: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExpectedPayment>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({ id: [id, string()] });
    req.appendTemplatePath`/api/expected_payments/${mapped.id}`;
    req.throwOn(404, ErrorMessageError, 'not found');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(expectedPaymentSchema, requestOptions);
  }

  /**
   * @param id           id
   * @param body
   * @return Response from the API call
   */
  async updateExpectedPayment(
    id: string,
    body?: ExpectedPaymentUpdateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExpectedPayment>> {
    const req = this.createRequest('PATCH');
    const mapped = req.prepareArgs({
      id: [id, string()],
      body: [body, optional(expectedPaymentUpdateRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/api/expected_payments/${mapped.id}`;
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(expectedPaymentSchema, requestOptions);
  }

  /**
   * @param id id
   * @return Response from the API call
   */
  async deleteExpectedPayment(
    id: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExpectedPayment>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({ id: [id, string()] });
    req.appendTemplatePath`/api/expected_payments/${mapped.id}`;
    req.throwOn(422, ErrorMessageError, 'parameter_invalid');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(expectedPaymentSchema, requestOptions);
  }
}
