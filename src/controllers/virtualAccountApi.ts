/**
 * ModernTreasury
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  VirtualAccount,
  virtualAccountSchema,
} from '../models/virtualAccount.js';
import {
  VirtualAccountCreateRequest,
  virtualAccountCreateRequestSchema,
} from '../models/virtualAccountCreateRequest.js';
import {
  VirtualAccountUpdateRequest,
  virtualAccountUpdateRequestSchema,
} from '../models/virtualAccountUpdateRequest.js';
import { array, dict, nullable, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { ErrorMessageError } from '../errors/errorMessageError.js';

export class VirtualAccountApi extends BaseApi {
  /**
   * Get a list of virtual accounts.
   *
   * @param afterCursor
   * @param perPage
   * @param internalAccountId
   * @param counterpartyId
   * @param metadata            For example, if you want to query for records with metadata
   *                                                      key `Type` and value `Loan`, the query would be
   *                                                      `metadata%5BType%5D=Loan`. This encodes the query parameters.
   * @return Response from the API call
   */
  async listVirtualAccounts(
    afterCursor?: string | null,
    perPage?: number,
    internalAccountId?: string,
    counterpartyId?: string,
    metadata?: Record<string, string>,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<VirtualAccount[]>> {
    const req = this.createRequest('GET', '/api/virtual_accounts');
    const mapped = req.prepareArgs({
      afterCursor: [afterCursor, optional(nullable(string()))],
      perPage: [perPage, optional(number())],
      internalAccountId: [internalAccountId, optional(string())],
      counterpartyId: [counterpartyId, optional(string())],
      metadata: [metadata, optional(dict(string()))],
    });
    req.query('after_cursor', mapped.afterCursor);
    req.query('per_page', mapped.perPage);
    req.query('internal_account_id', mapped.internalAccountId);
    req.query('counterparty_id', mapped.counterpartyId);
    req.query('metadata', mapped.metadata);
    req.throwOn(401, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(array(virtualAccountSchema), requestOptions);
  }

  /**
   * @param idempotencyKey  This key should be something unique, preferably
   *                                                              something like an UUID.
   * @param body
   * @return Response from the API call
   */
  async createVirtualAccount(
    idempotencyKey?: string,
    body?: VirtualAccountCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<VirtualAccount>> {
    const req = this.createRequest('POST', '/api/virtual_accounts');
    const mapped = req.prepareArgs({
      idempotencyKey: [idempotencyKey, optional(string())],
      body: [body, optional(virtualAccountCreateRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.header('Idempotency-Key', mapped.idempotencyKey);
    req.json(mapped.body);
    req.throwOn(401, ErrorMessageError, 'unauthorized');
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(virtualAccountSchema, requestOptions);
  }

  /**
   * @param id Virtual Acccount ID
   * @return Response from the API call
   */
  async getVirtualAccount(
    id: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<VirtualAccount>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({ id: [id, string()] });
    req.appendTemplatePath`/api/virtual_accounts/${mapped.id}`;
    req.throwOn(404, ErrorMessageError, 'not found');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(virtualAccountSchema, requestOptions);
  }

  /**
   * @param id           Virtual Acccount ID
   * @param body
   * @return Response from the API call
   */
  async updateVirtualAccount(
    id: string,
    body?: VirtualAccountUpdateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<VirtualAccount>> {
    const req = this.createRequest('PATCH');
    const mapped = req.prepareArgs({
      id: [id, string()],
      body: [body, optional(virtualAccountUpdateRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/api/virtual_accounts/${mapped.id}`;
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(virtualAccountSchema, requestOptions);
  }

  /**
   * @param id Virtual Acccount ID
   * @return Response from the API call
   */
  async deleteVirtualAccount(
    id: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<VirtualAccount>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({ id: [id, string()] });
    req.appendTemplatePath`/api/virtual_accounts/${mapped.id}`;
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(virtualAccountSchema, requestOptions);
  }
}
