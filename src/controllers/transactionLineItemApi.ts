/**
 * Modern TreasuryLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  TransactionLineItem,
  transactionLineItemSchema,
} from '../models/transactionLineItem.js';
import { Type16, type16Schema } from '../models/type16.js';
import { array, nullable, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';

export class TransactionLineItemApi extends BaseApi {
  /**
   * @param transactionId  transaction_id
   * @param afterCursor
   * @param type
   * @param perPage
   * @return Response from the API call
   */
  async listTransactionLineItems(
    transactionId: string,
    afterCursor?: string | null,
    type?: Type16 | null,
    perPage?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TransactionLineItem[]>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      transactionId: [transactionId, string()],
      afterCursor: [afterCursor, optional(nullable(string()))],
      type: [type, optional(nullable(type16Schema))],
      perPage: [perPage, optional(number())],
    });
    req.query('after_cursor', mapped.afterCursor);
    req.query('type', mapped.type);
    req.query('per_page', mapped.perPage);
    req.appendTemplatePath`/api/transactions/${mapped.transactionId}/line_items`;
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(array(transactionLineItemSchema), requestOptions);
  }
}
