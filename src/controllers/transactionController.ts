/**
 * Modern TreasuryLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  IncomingPaymentDetailUpdateRequest,
  incomingPaymentDetailUpdateRequestSchema,
} from '../models/incomingPaymentDetailUpdateRequest.js';
import { Transaction, transactionSchema } from '../models/transaction.js';
import {
  array,
  boolean,
  dict,
  nullable,
  number,
  optional,
  string,
} from '../schema.js';
import { BaseController } from './baseController.js';
import { ErrorMessageError } from '../errors/errorMessageError.js';

export class TransactionController extends BaseController {
  /**
   * Get a list of all transactions.
   *
   * @param afterCursor
   * @param perPage
   * @param internalAccountId   Specify `internal_account_id` if you wish to see
   *                                                      transactions to/from a specific account.
   * @param virtualAccountId
   * @param posted              Either `true` or `false`.
   * @param asOfDateStart       Filters transactions with an `as_of_date` starting on or
   *                                                      after the specified date (YYYY-MM-DD).
   * @param asOfDateEnd         Filters transactions with an `as_of_date` starting on or
   *                                                      before the specified date (YYYY-MM-DD).
   * @param direction
   * @param counterpartyId
   * @param paymentType
   * @param transactableType
   * @param description         Filters for transactions including the queried string in the
   *                                                      description.
   * @param vendorId            Filters for transactions including the queried vendor id (an
   *                                                      identifier given to transactions by the bank).
   * @param metadata            For example, if you want to query for records with metadata
   *                                                      key `Type` and value `Loan`, the query would be
   *                                                      `metadata%5BType%5D=Loan`. This encodes the query parameters.
   * @return Response from the API call
   */
  async listTransactions(
    afterCursor?: string | null,
    perPage?: number,
    internalAccountId?: string,
    virtualAccountId?: string,
    posted?: boolean,
    asOfDateStart?: string,
    asOfDateEnd?: string,
    direction?: string,
    counterpartyId?: string,
    paymentType?: string,
    transactableType?: string,
    description?: string,
    vendorId?: string,
    metadata?: Record<string, string>,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Transaction[]>> {
    const req = this.createRequest('GET', '/api/transactions');
    const mapped = req.prepareArgs({
      afterCursor: [afterCursor, optional(nullable(string()))],
      perPage: [perPage, optional(number())],
      internalAccountId: [internalAccountId, optional(string())],
      virtualAccountId: [virtualAccountId, optional(string())],
      posted: [posted, optional(boolean())],
      asOfDateStart: [asOfDateStart, optional(string())],
      asOfDateEnd: [asOfDateEnd, optional(string())],
      direction: [direction, optional(string())],
      counterpartyId: [counterpartyId, optional(string())],
      paymentType: [paymentType, optional(string())],
      transactableType: [transactableType, optional(string())],
      description: [description, optional(string())],
      vendorId: [vendorId, optional(string())],
      metadata: [metadata, optional(dict(string()))],
    });
    req.query('after_cursor', mapped.afterCursor);
    req.query('per_page', mapped.perPage);
    req.query('internal_account_id', mapped.internalAccountId);
    req.query('virtual_account_id', mapped.virtualAccountId);
    req.query('posted', mapped.posted);
    req.query('as_of_date_start', mapped.asOfDateStart);
    req.query('as_of_date_end', mapped.asOfDateEnd);
    req.query('direction', mapped.direction);
    req.query('counterparty_id', mapped.counterpartyId);
    req.query('payment_type', mapped.paymentType);
    req.query('transactable_type', mapped.transactableType);
    req.query('description', mapped.description);
    req.query('vendor_id', mapped.vendorId);
    req.query('metadata', mapped.metadata);
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(array(transactionSchema), requestOptions);
  }

  /**
   * Get details on a single transaction.
   *
   * @param id Transaction ID
   * @return Response from the API call
   */
  async getTransaction(
    id: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Transaction>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({ id: [id, string()] });
    req.appendTemplatePath`/api/transactions/${mapped.id}`;
    req.throwOn(404, ErrorMessageError, 'not found');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(transactionSchema, requestOptions);
  }

  /**
   * Update a single transaction.
   *
   * @param id           Transaction ID
   * @param body
   * @return Response from the API call
   */
  async updateTransaction(
    id: string,
    body?: IncomingPaymentDetailUpdateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Transaction>> {
    const req = this.createRequest('PATCH');
    const mapped = req.prepareArgs({
      id: [id, string()],
      body: [body, optional(incomingPaymentDetailUpdateRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/api/transactions/${mapped.id}`;
    req.throwOn(404, ErrorMessageError, 'not found');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(transactionSchema, requestOptions);
  }
}
