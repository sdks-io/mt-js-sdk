/**
 * ModernTreasury
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  ExternalAccount,
  externalAccountSchema,
} from '../models/externalAccount.js';
import {
  ExternalAccountCompleteVerificationRequest,
  externalAccountCompleteVerificationRequestSchema,
} from '../models/externalAccountCompleteVerificationRequest.js';
import {
  ExternalAccountCreateRequest,
  externalAccountCreateRequestSchema,
} from '../models/externalAccountCreateRequest.js';
import {
  ExternalAccountUpdateRequest,
  externalAccountUpdateRequestSchema,
} from '../models/externalAccountUpdateRequest.js';
import {
  ExternalAccountVerifyRequest,
  externalAccountVerifyRequestSchema,
} from '../models/externalAccountVerifyRequest.js';
import { array, dict, nullable, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { ErrorMessageError } from '../errors/errorMessageError.js';

export class ExternalAccountApi extends BaseApi {
  /**
   * @param id              external account id
   * @param idempotencyKey  This key should be something unique, preferably
   *                                                               something like an UUID.
   * @param body
   * @return Response from the API call
   */
  async verifyExternalAccount(
    id: string,
    idempotencyKey?: string,
    body?: ExternalAccountVerifyRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExternalAccount>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      id: [id, string()],
      idempotencyKey: [idempotencyKey, optional(string())],
      body: [body, optional(externalAccountVerifyRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.header('Idempotency-Key', mapped.idempotencyKey);
    req.json(mapped.body);
    req.appendTemplatePath`/api/external_accounts/${mapped.id}/verify`;
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(externalAccountSchema, requestOptions);
  }

  /**
   * @param id              external account id
   * @param idempotencyKey  This key should be something unique,
   *                                                                             preferably something like an UUID.
   * @param body
   * @return Response from the API call
   */
  async completeVerificationExternalAccount(
    id: string,
    idempotencyKey?: string,
    body?: ExternalAccountCompleteVerificationRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExternalAccount>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      id: [id, string()],
      idempotencyKey: [idempotencyKey, optional(string())],
      body: [body, optional(externalAccountCompleteVerificationRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.header('Idempotency-Key', mapped.idempotencyKey);
    req.json(mapped.body);
    req.appendTemplatePath`/api/external_accounts/${mapped.id}/complete_verification`;
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(externalAccountSchema, requestOptions);
  }

  /**
   * @param afterCursor
   * @param perPage
   * @param partyName       Searches the ExternalAccount's party_name AND the Counterparty's
   *                                                  party_name
   * @param counterpartyId
   * @param metadata        For example, if you want to query for records with metadata key
   *                                                  `Type` and value `Loan`, the query would be
   *                                                  `metadata%5BType%5D=Loan`. This encodes the query parameters.
   * @return Response from the API call
   */
  async listExternalAccounts(
    afterCursor?: string | null,
    perPage?: number,
    partyName?: string,
    counterpartyId?: string,
    metadata?: Record<string, string>,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExternalAccount[]>> {
    const req = this.createRequest('GET', '/api/external_accounts');
    const mapped = req.prepareArgs({
      afterCursor: [afterCursor, optional(nullable(string()))],
      perPage: [perPage, optional(number())],
      partyName: [partyName, optional(string())],
      counterpartyId: [counterpartyId, optional(string())],
      metadata: [metadata, optional(dict(string()))],
    });
    req.query('after_cursor', mapped.afterCursor);
    req.query('per_page', mapped.perPage);
    req.query('party_name', mapped.partyName);
    req.query('counterparty_id', mapped.counterpartyId);
    req.query('metadata', mapped.metadata);
    req.throwOn(401, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(array(externalAccountSchema), requestOptions);
  }

  /**
   * @param idempotencyKey  This key should be something unique, preferably
   *                                                               something like an UUID.
   * @param body
   * @return Response from the API call
   */
  async createExternalAccount(
    idempotencyKey?: string,
    body?: ExternalAccountCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExternalAccount>> {
    const req = this.createRequest('POST', '/api/external_accounts');
    const mapped = req.prepareArgs({
      idempotencyKey: [idempotencyKey, optional(string())],
      body: [body, optional(externalAccountCreateRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.header('Idempotency-Key', mapped.idempotencyKey);
    req.json(mapped.body);
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(externalAccountSchema, requestOptions);
  }

  /**
   * @param id external account id
   * @return Response from the API call
   */
  async getExternalAccount(
    id: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExternalAccount>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({ id: [id, string()] });
    req.appendTemplatePath`/api/external_accounts/${mapped.id}`;
    req.throwOn(404, ErrorMessageError, 'not found');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(externalAccountSchema, requestOptions);
  }

  /**
   * @param id           external account id
   * @param body
   * @return Response from the API call
   */
  async updateExternalAccount(
    id: string,
    body?: ExternalAccountUpdateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExternalAccount>> {
    const req = this.createRequest('PATCH');
    const mapped = req.prepareArgs({
      id: [id, string()],
      body: [body, optional(externalAccountUpdateRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/api/external_accounts/${mapped.id}`;
    req.throwOn(404, ErrorMessageError, 'not found');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(externalAccountSchema, requestOptions);
  }

  /**
   * @param id external account id
   * @return Response from the API call
   */
  async deleteExternalAccount(
    id: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({ id: [id, string()] });
    req.appendTemplatePath`/api/external_accounts/${mapped.id}`;
    req.authenticate([{ basicAuth: true }]);
    return req.call(requestOptions);
  }
}
