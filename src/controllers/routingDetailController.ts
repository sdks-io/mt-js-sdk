/**
 * Modern TreasuryLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  AccountsType1Enum,
  accountsType1EnumSchema,
} from '../models/accountsType1Enum.js';
import {
  AccountsTypeEnum,
  accountsTypeEnumSchema,
} from '../models/accountsTypeEnum.js';
import { RoutingDetail, routingDetailSchema } from '../models/routingDetail.js';
import {
  RoutingDetailCreateRequest,
  routingDetailCreateRequestSchema,
} from '../models/routingDetailCreateRequest.js';
import { array, nullable, number, optional, string } from '../schema.js';
import { BaseController } from './baseController.js';
import { ErrorMessageError } from '../errors/errorMessageError.js';

export class RoutingDetailController extends BaseController {
  /**
   * Get a list of routing details for a single internal or external account.
   *
   * @param accountsType
   * @param accountId     The ID of the account.
   * @param afterCursor
   * @param perPage
   * @return Response from the API call
   */
  async listRoutingDetails(
    accountsType: AccountsTypeEnum,
    accountId: string,
    afterCursor?: string | null,
    perPage?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<RoutingDetail[]>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      accountsType: [accountsType, accountsTypeEnumSchema],
      accountId: [accountId, string()],
      afterCursor: [afterCursor, optional(nullable(string()))],
      perPage: [perPage, optional(number())],
    });
    req.query('after_cursor', mapped.afterCursor);
    req.query('per_page', mapped.perPage);
    req.appendTemplatePath`/api/${mapped.accountsType}/${mapped.accountId}/routing_details`;
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(array(routingDetailSchema), requestOptions);
  }

  /**
   * Create a routing detail for a single external account.
   *
   * @param accountsType
   * @param accountId       The ID of the account.
   * @param idempotencyKey  This key should be something unique, preferably
   *                                                             something like an UUID.
   * @param body
   * @return Response from the API call
   */
  async createRoutingDetail(
    accountsType: AccountsType1Enum,
    accountId: string,
    idempotencyKey?: string,
    body?: RoutingDetailCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<RoutingDetail>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      accountsType: [accountsType, accountsType1EnumSchema],
      accountId: [accountId, string()],
      idempotencyKey: [idempotencyKey, optional(string())],
      body: [body, optional(routingDetailCreateRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.header('Idempotency-Key', mapped.idempotencyKey);
    req.json(mapped.body);
    req.appendTemplatePath`/api/${mapped.accountsType}/${mapped.accountId}/routing_details`;
    req.throwOn(404, ErrorMessageError, 'unsuccessful');
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(routingDetailSchema, requestOptions);
  }

  /**
   * Get a single routing detail for a single internal or external account.
   *
   * @param accountsType
   * @param accountId     The ID of the account.
   * @param id            The ID of the routing detail.
   * @return Response from the API call
   */
  async getRoutingDetail(
    accountsType: AccountsTypeEnum,
    accountId: string,
    id: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<RoutingDetail>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      accountsType: [accountsType, accountsTypeEnumSchema],
      accountId: [accountId, string()],
      id: [id, string()],
    });
    req.appendTemplatePath`/api/${mapped.accountsType}/${mapped.accountId}/routing_details/${mapped.id}`;
    req.throwOn(404, ErrorMessageError, 'not found');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(routingDetailSchema, requestOptions);
  }

  /**
   * Delete a routing detail for a single external account.
   *
   * @param accountsType
   * @param accountId     The ID of the account.
   * @param id            The ID of the routing detail.
   * @return Response from the API call
   */
  async deleteRoutingDetail(
    accountsType: AccountsType1Enum,
    accountId: string,
    id: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({
      accountsType: [accountsType, accountsType1EnumSchema],
      accountId: [accountId, string()],
      id: [id, string()],
    });
    req.appendTemplatePath`/api/${mapped.accountsType}/${mapped.accountId}/routing_details/${mapped.id}`;
    req.authenticate([{ basicAuth: true }]);
    return req.call(requestOptions);
  }
}
