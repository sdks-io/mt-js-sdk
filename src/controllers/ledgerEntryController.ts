/**
 * Modern TreasuryLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import { Direction15, direction15Schema } from '../models/direction15.js';
import { LedgerEntry, ledgerEntrySchema } from '../models/ledgerEntry.js';
import { OrderBy, orderBySchema } from '../models/orderBy.js';
import { Status22, status22Schema } from '../models/status22.js';
import {
  array,
  boolean,
  dict,
  nullable,
  number,
  optional,
  string,
} from '../schema.js';
import { BaseController } from './baseController.js';
import { ErrorMessageError } from '../errors/errorMessageError.js';

export class LedgerEntryController extends BaseController {
  /**
   * Get a list of all ledger entries.
   *
   * @param afterCursor
   * @param perPage
   * @param id
   * @param ledgerAccountId
   * @param ledgerTransactionId
   * @param ledgerAccountPayoutId
   * @param effectiveDate               Use `gt` (>), `gte` (>=), `lt` (<), `lte` (<=), or
   *                                                              `eq` (=) to filter by the transaction's effective
   *                                                              date. Format YYYY-MM-DD
   * @param effectiveAt                 Use `gt` (>), `gte` (>=), `lt` (<), `lte` (<=), or
   *                                                              `eq` (=) to filter by the transaction's effective
   *                                                              time. Format ISO8601
   * @param updatedAt                   Use `gt` (>), `gte` (>=), `lt` (<), `lte` (<=), or
   *                                                              `eq` (=) to filter by the posted at timestamp. For
   *                                                              example, for all times after Jan 1 2000 12:00 UTC,
   *                                                              use updated_at%5Bgt%5D=2000-01-01T12:00:00Z.
   * @param asOfLockVersion             Shows all ledger entries that were present on a
   *                                                              ledger account at a particular `lock_version`. You
   *                                                              must also specify `ledger_account_id`.
   * @param ledgerAccountLockVersion    Use `gt` (>), `gte` (>=), `lt` (<), `lte` (<=), or
   *                                                              `eq` (=) to filter by the lock_version of a ledger
   *                                                              account. For example, for all entries created at or
   *                                                              before before lock_version 1000 of a ledger account,
   *                                                              use `ledger_account_lock_version%5Blte%5D=1000`.
   * @param ledgerAccountCategoryId     Get all ledger entries that match the direction
   *                                                              specified. One of `credit`, `debit`.
   * @param ledgerAccountStatementId    Get all ledger entries that are included in the
   *                                                              ledger account statement.
   * @param showDeleted                 If true, response will include ledger entries that
   *                                                              were deleted. When you update a ledger transaction to
   *                                                              specify a new set of entries, the previous entries
   *                                                              are deleted.
   * @param direction                   If true, response will include ledger entries that
   *                                                              were deleted. When you update a ledger transaction to
   *                                                              specify a new set of entries, the previous entries
   *                                                              are deleted.
   * @param status                      Get all ledger entries that match the status
   *                                                              specified. One of `pending`, `posted`, or `archived`.
   * @param orderBy                     Order by `created_at` or `effective_at` in `asc` or
   *                                                              `desc` order. For example, to order by `effective_at
   *                                                              asc`, use `order_by%5Beffective_at%5D=asc`. Ordering
   *                                                              by only one field at a time is supported.
   * @param showBalances                If true, response will include the balances attached
   *                                                              to the ledger entry. If there is no balance available,
   *                                                              null will be returned instead.
   * @param metadata                    For example, if you want to query for records with
   *                                                              metadata key `Type` and value `Loan`, the query would
   *                                                              be `metadata%5BType%5D=Loan`. This encodes the query
   *                                                              parameters.
   * @return Response from the API call
   */
  async listLedgerEntries(
    afterCursor?: string | null,
    perPage?: number,
    id?: Record<string, string>,
    ledgerAccountId?: string,
    ledgerTransactionId?: string,
    ledgerAccountPayoutId?: string,
    effectiveDate?: Record<string, string>,
    effectiveAt?: Record<string, string>,
    updatedAt?: Record<string, string>,
    asOfLockVersion?: number,
    ledgerAccountLockVersion?: Record<string, number>,
    ledgerAccountCategoryId?: string,
    ledgerAccountStatementId?: string,
    showDeleted?: boolean,
    direction?: Direction15,
    status?: Status22,
    orderBy?: OrderBy,
    showBalances?: boolean,
    metadata?: Record<string, string>,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LedgerEntry[]>> {
    const req = this.createRequest('GET', '/api/ledger_entries');
    const mapped = req.prepareArgs({
      afterCursor: [afterCursor, optional(nullable(string()))],
      perPage: [perPage, optional(number())],
      id: [id, optional(dict(string()))],
      ledgerAccountId: [ledgerAccountId, optional(string())],
      ledgerTransactionId: [ledgerTransactionId, optional(string())],
      ledgerAccountPayoutId: [ledgerAccountPayoutId, optional(string())],
      effectiveDate: [effectiveDate, optional(dict(string()))],
      effectiveAt: [effectiveAt, optional(dict(string()))],
      updatedAt: [updatedAt, optional(dict(string()))],
      asOfLockVersion: [asOfLockVersion, optional(number())],
      ledgerAccountLockVersion: [
        ledgerAccountLockVersion,
        optional(dict(number())),
      ],
      ledgerAccountCategoryId: [ledgerAccountCategoryId, optional(string())],
      ledgerAccountStatementId: [ledgerAccountStatementId, optional(string())],
      showDeleted: [showDeleted, optional(boolean())],
      direction: [direction, optional(direction15Schema)],
      status: [status, optional(status22Schema)],
      orderBy: [orderBy, optional(orderBySchema)],
      showBalances: [showBalances, optional(boolean())],
      metadata: [metadata, optional(dict(string()))],
    });
    req.query('after_cursor', mapped.afterCursor);
    req.query('per_page', mapped.perPage);
    req.query('id', mapped.id);
    req.query('ledger_account_id', mapped.ledgerAccountId);
    req.query('ledger_transaction_id', mapped.ledgerTransactionId);
    req.query('ledger_account_payout_id', mapped.ledgerAccountPayoutId);
    req.query('effective_date', mapped.effectiveDate);
    req.query('effective_at', mapped.effectiveAt);
    req.query('updated_at', mapped.updatedAt);
    req.query('as_of_lock_version', mapped.asOfLockVersion);
    req.query('ledger_account_lock_version', mapped.ledgerAccountLockVersion);
    req.query('ledger_account_category_id', mapped.ledgerAccountCategoryId);
    req.query('ledger_account_statement_id', mapped.ledgerAccountStatementId);
    req.query('show_deleted', mapped.showDeleted);
    req.query('direction', mapped.direction);
    req.query('status', mapped.status);
    req.query('order_by', mapped.orderBy);
    req.query('show_balances', mapped.showBalances);
    req.query('metadata', mapped.metadata);
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(array(ledgerEntrySchema), requestOptions);
  }

  /**
   * Get details on a single ledger entry.
   *
   * @param id            id
   * @param showBalances  If true, response will include the balances attached to the ledger entry. If
   *                                 there is no balance available, null will be returned instead.
   * @return Response from the API call
   */
  async getLedgerEntry(
    id: string,
    showBalances?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LedgerEntry>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      id: [id, string()],
      showBalances: [showBalances, optional(boolean())],
    });
    req.query('show_balances', mapped.showBalances);
    req.appendTemplatePath`/api/ledger_entries/${mapped.id}`;
    req.throwOn(404, ErrorMessageError, 'not found');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(ledgerEntrySchema, requestOptions);
  }
}
