/**
 * Modern TreasuryLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import { AsyncResponse, asyncResponseSchema } from '../models/asyncResponse.js';
import { Direction15, direction15Schema } from '../models/direction15.js';
import {
  IncomingPaymentDetail,
  incomingPaymentDetailSchema,
} from '../models/incomingPaymentDetail.js';
import {
  IncomingPaymentDetailCreateRequest,
  incomingPaymentDetailCreateRequestSchema,
} from '../models/incomingPaymentDetailCreateRequest.js';
import {
  IncomingPaymentDetailUpdateRequest,
  incomingPaymentDetailUpdateRequestSchema,
} from '../models/incomingPaymentDetailUpdateRequest.js';
import { Status21, status21Schema } from '../models/status21.js';
import { Type14, type14Schema } from '../models/type14.js';
import { array, dict, nullable, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { ErrorMessageError } from '../errors/errorMessageError.js';

export class IncomingPaymentDetailApi extends BaseApi {
  /**
   * Get a list of Incoming Payment Details.
   *
   * @param afterCursor
   * @param perPage
   * @param direction          One of `credit` or `debit`.
   * @param status             The current status of the incoming payment order. One of
   *                                                     `pending`, `completed`, or `returned`.
   * @param type               One of: `ach`, `book`, `check`, `eft`, `interac`, `rtp`,
   *                                                     `sepa`, `signet`, or `wire`.
   * @param asOfDateStart      Filters incoming payment details with an as_of_date starting
   *                                                     on or after the specified date (YYYY-MM-DD).
   * @param asOfDateEnd        Filters incoming payment details with an as_of_date starting
   *                                                     on or before the specified date (YYYY-MM-DD).
   * @param metadata           For example, if you want to query for records with metadata
   *                                                     key `Type` and value `Loan`, the query would be
   *                                                     `metadata%5BType%5D=Loan`. This encodes the query parameters.
   * @param virtualAccountId   If the incoming payment detail is in a virtual account, the
   *                                                     ID of the Virtual Account.
   * @return Response from the API call
   */
  async listIncomingPaymentDetails(
    afterCursor?: string | null,
    perPage?: number,
    direction?: Direction15,
    status?: Status21,
    type?: Type14,
    asOfDateStart?: string,
    asOfDateEnd?: string,
    metadata?: Record<string, string>,
    virtualAccountId?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IncomingPaymentDetail[]>> {
    const req = this.createRequest('GET', '/api/incoming_payment_details');
    const mapped = req.prepareArgs({
      afterCursor: [afterCursor, optional(nullable(string()))],
      perPage: [perPage, optional(number())],
      direction: [direction, optional(direction15Schema)],
      status: [status, optional(status21Schema)],
      type: [type, optional(type14Schema)],
      asOfDateStart: [asOfDateStart, optional(string())],
      asOfDateEnd: [asOfDateEnd, optional(string())],
      metadata: [metadata, optional(dict(string()))],
      virtualAccountId: [virtualAccountId, optional(string())],
    });
    req.query('after_cursor', mapped.afterCursor);
    req.query('per_page', mapped.perPage);
    req.query('direction', mapped.direction);
    req.query('status', mapped.status);
    req.query('type', mapped.type);
    req.query('as_of_date_start', mapped.asOfDateStart);
    req.query('as_of_date_end', mapped.asOfDateEnd);
    req.query('metadata', mapped.metadata);
    req.query('virtual_account_id', mapped.virtualAccountId);
    req.throwOn(401, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(array(incomingPaymentDetailSchema), requestOptions);
  }

  /**
   * Get an existing Incoming Payment Detail.
   *
   * @param id The unique identifier of the incoming payment detail.
   * @return Response from the API call
   */
  async getIncomingPaymentDetail(
    id: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IncomingPaymentDetail>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({ id: [id, string()] });
    req.appendTemplatePath`/api/incoming_payment_details/${mapped.id}`;
    req.throwOn(404, ErrorMessageError, 'not found');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(incomingPaymentDetailSchema, requestOptions);
  }

  /**
   * Update an existing Incoming Payment Detail.
   *
   * @param id           The unique identifier of the incoming payment
   *                                                                  detail.
   * @param body
   * @return Response from the API call
   */
  async updateIncomingPaymentDetail(
    id: string,
    body?: IncomingPaymentDetailUpdateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IncomingPaymentDetail>> {
    const req = this.createRequest('PATCH');
    const mapped = req.prepareArgs({
      id: [id, string()],
      body: [body, optional(incomingPaymentDetailUpdateRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/api/incoming_payment_details/${mapped.id}`;
    req.throwOn(404, ErrorMessageError, 'not found');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(incomingPaymentDetailSchema, requestOptions);
  }

  /**
   * Simulate Incoming Payment Detail
   *
   * @param idempotencyKey  This key should be something unique,
   *                                                                     preferably something like an UUID.
   * @param body
   * @return Response from the API call
   */
  async createAsyncIncomingPaymentDetail(
    idempotencyKey?: string,
    body?: IncomingPaymentDetailCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<AsyncResponse>> {
    const req = this.createRequest(
      'POST',
      '/api/simulations/incoming_payment_details/create_async'
    );
    const mapped = req.prepareArgs({
      idempotencyKey: [idempotencyKey, optional(string())],
      body: [body, optional(incomingPaymentDetailCreateRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.header('Idempotency-Key', mapped.idempotencyKey);
    req.json(mapped.body);
    req.throwOn(404, ErrorMessageError, 'not found');
    req.throwOn(405, ErrorMessageError, 'not allowed');
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(asyncResponseSchema, requestOptions);
  }
}
