/**
 * ModernTreasury
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import { Invoice, invoiceSchema } from '../models/invoice.js';
import {
  InvoiceCreateRequest,
  invoiceCreateRequestSchema,
} from '../models/invoiceCreateRequest.js';
import {
  InvoiceUpdateRequest,
  invoiceUpdateRequestSchema,
} from '../models/invoiceUpdateRequest.js';
import { array, nullable, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';

export class InvoiceApi extends BaseApi {
  /**
   * @param afterCursor
   * @param perPage
   * @return Response from the API call
   */
  async listInvoices(
    afterCursor?: string | null,
    perPage?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Invoice[]>> {
    const req = this.createRequest('GET', '/api/invoices');
    const mapped = req.prepareArgs({
      afterCursor: [afterCursor, optional(nullable(string()))],
      perPage: [perPage, optional(number())],
    });
    req.query('after_cursor', mapped.afterCursor);
    req.query('per_page', mapped.perPage);
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(array(invoiceSchema), requestOptions);
  }

  /**
   * @param idempotencyKey  This key should be something unique, preferably something
   *                                                       like an UUID.
   * @param body
   * @return Response from the API call
   */
  async createInvoice(
    idempotencyKey?: string,
    body?: InvoiceCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Invoice>> {
    const req = this.createRequest('POST', '/api/invoices');
    const mapped = req.prepareArgs({
      idempotencyKey: [idempotencyKey, optional(string())],
      body: [body, optional(invoiceCreateRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.header('Idempotency-Key', mapped.idempotencyKey);
    req.json(mapped.body);
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(invoiceSchema, requestOptions);
  }

  /**
   * @param id id
   * @return Response from the API call
   */
  async getInvoice(
    id: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Invoice>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({ id: [id, string()] });
    req.appendTemplatePath`/api/invoices/${mapped.id}`;
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(invoiceSchema, requestOptions);
  }

  /**
   * @param id           id
   * @param body
   * @return Response from the API call
   */
  async updateInvoice(
    id: string,
    body?: InvoiceUpdateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Invoice>> {
    const req = this.createRequest('PATCH');
    const mapped = req.prepareArgs({
      id: [id, string()],
      body: [body, optional(invoiceUpdateRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/api/invoices/${mapped.id}`;
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(invoiceSchema, requestOptions);
  }
}
