/**
 * ModernTreasury
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import { Balances, balancesSchema } from '../models/balances.js';
import { Balances1, balances1Schema } from '../models/balances1.js';
import {
  LedgerAccountCategory,
  ledgerAccountCategorySchema,
} from '../models/ledgerAccountCategory.js';
import {
  LedgerAccountCategoryCreateRequest,
  ledgerAccountCategoryCreateRequestSchema,
} from '../models/ledgerAccountCategoryCreateRequest.js';
import {
  LedgerAccountCategoryUpdateRequest,
  ledgerAccountCategoryUpdateRequestSchema,
} from '../models/ledgerAccountCategoryUpdateRequest.js';
import { array, dict, nullable, number, optional, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { ErrorMessageError } from '../errors/errorMessageError.js';

export class LedgerAccountCategoryApi extends BaseApi {
  /**
   * Get a list of ledger account categories.
   *
   * @param afterCursor
   * @param perPage
   * @param metadata                          For example, if you want to query for records
   *                                                                    with metadata key `Type` and value `Loan`, the
   *                                                                    query would be `metadata%5BType%5D=Loan`. This
   *                                                                    encodes the query parameters.
   * @param name
   * @param ledgerId
   * @param parentLedgerAccountCategoryId     Query categories that are nested underneath a
   *                                                                    parent category
   * @param ledgerAccountId                   Query categories which contain a ledger
   *                                                                    account directly or through child categories.
   * @param balances                          For example, if you want the balances as of a
   *                                                                    particular time (ISO8601), the encoded query
   *                                                                    string would be
   *                                                                    `balances%5Beffective_at%5D=2000-12-31T12:00:
   *                                                                    00Z`. The balances as of a time are inclusive
   *                                                                    of entries with that exact time.
   * @return Response from the API call
   */
  async listLedgerAccountCategories(
    afterCursor?: string | null,
    perPage?: number,
    metadata?: Record<string, string>,
    name?: string,
    ledgerId?: string,
    parentLedgerAccountCategoryId?: string,
    ledgerAccountId?: string,
    balances?: Balances,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LedgerAccountCategory[]>> {
    const req = this.createRequest('GET', '/api/ledger_account_categories');
    const mapped = req.prepareArgs({
      afterCursor: [afterCursor, optional(nullable(string()))],
      perPage: [perPage, optional(number())],
      metadata: [metadata, optional(dict(string()))],
      name: [name, optional(string())],
      ledgerId: [ledgerId, optional(string())],
      parentLedgerAccountCategoryId: [
        parentLedgerAccountCategoryId,
        optional(string()),
      ],
      ledgerAccountId: [ledgerAccountId, optional(string())],
      balances: [balances, optional(balancesSchema)],
    });
    req.query('after_cursor', mapped.afterCursor);
    req.query('per_page', mapped.perPage);
    req.query('metadata', mapped.metadata);
    req.query('name', mapped.name);
    req.query('ledger_id', mapped.ledgerId);
    req.query(
      'parent_ledger_account_category_id',
      mapped.parentLedgerAccountCategoryId
    );
    req.query('ledger_account_id', mapped.ledgerAccountId);
    req.query('balances', mapped.balances);
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(array(ledgerAccountCategorySchema), requestOptions);
  }

  /**
   * Create a ledger account category.
   *
   * @param idempotencyKey  This key should be something unique,
   *                                                                     preferably something like an UUID.
   * @param body
   * @return Response from the API call
   */
  async createLedgerAccountCategory(
    idempotencyKey?: string,
    body?: LedgerAccountCategoryCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LedgerAccountCategory>> {
    const req = this.createRequest('POST', '/api/ledger_account_categories');
    const mapped = req.prepareArgs({
      idempotencyKey: [idempotencyKey, optional(string())],
      body: [body, optional(ledgerAccountCategoryCreateRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.header('Idempotency-Key', mapped.idempotencyKey);
    req.json(mapped.body);
    req.throwOn(403, ErrorMessageError, 'forbidden');
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(ledgerAccountCategorySchema, requestOptions);
  }

  /**
   * Get the details on a single ledger account category.
   *
   * @param id       id
   * @param balances For example, if you want the balances as of a particular time (ISO8601), the
   *                                     encoded query string would be `balances%5Beffective_at%5D=2000-12-31T12:00:
   *                                     00Z`. The balances as of a time are inclusive of entries with that exact time.
   * @return Response from the API call
   */
  async getLedgerAccountCategory(
    id: string,
    balances?: Balances1,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LedgerAccountCategory>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      id: [id, string()],
      balances: [balances, optional(balances1Schema)],
    });
    req.query('balances', mapped.balances);
    req.appendTemplatePath`/api/ledger_account_categories/${mapped.id}`;
    req.throwOn(404, ErrorMessageError, 'not found');
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(ledgerAccountCategorySchema, requestOptions);
  }

  /**
   * Update the details of a ledger account category.
   *
   * @param id           id
   * @param body
   * @return Response from the API call
   */
  async updateLedgerAccountCategory(
    id: string,
    body?: LedgerAccountCategoryUpdateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LedgerAccountCategory>> {
    const req = this.createRequest('PATCH');
    const mapped = req.prepareArgs({
      id: [id, string()],
      body: [body, optional(ledgerAccountCategoryUpdateRequestSchema)],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/api/ledger_account_categories/${mapped.id}`;
    req.throwOn(403, ErrorMessageError, 'forbidden');
    req.throwOn(404, ErrorMessageError, 'not found');
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(ledgerAccountCategorySchema, requestOptions);
  }

  /**
   * Delete a ledger account category.
   *
   * @param id id
   * @return Response from the API call
   */
  async deleteLedgerAccountCategory(
    id: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LedgerAccountCategory>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({ id: [id, string()] });
    req.appendTemplatePath`/api/ledger_account_categories/${mapped.id}`;
    req.throwOn(403, ErrorMessageError, 'forbidden');
    req.throwOn(404, ErrorMessageError, 'not found');
    req.authenticate([{ basicAuth: true }]);
    return req.callAsJson(ledgerAccountCategorySchema, requestOptions);
  }

  /**
   * Add a ledger account to a ledger account category.
   *
   * @param id                id
   * @param ledgerAccountId   ledger_account_id
   * @return Response from the API call
   */
  async addLedgerAccountToLedgerAccountCategory(
    id: string,
    ledgerAccountId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      id: [id, string()],
      ledgerAccountId: [ledgerAccountId, string()],
    });
    req.appendTemplatePath`/api/ledger_account_categories/${mapped.id}/ledger_accounts/${mapped.ledgerAccountId}`;
    req.throwOn(403, ErrorMessageError, 'forbidden');
    req.throwOn(404, ErrorMessageError, 'not found');
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.call(requestOptions);
  }

  /**
   * Remove a ledger account from a ledger account category.
   *
   * @param id                id
   * @param ledgerAccountId   ledger_account_id
   * @return Response from the API call
   */
  async removeLedgerAccountFromLedgerAccountCategory(
    id: string,
    ledgerAccountId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({
      id: [id, string()],
      ledgerAccountId: [ledgerAccountId, string()],
    });
    req.appendTemplatePath`/api/ledger_account_categories/${mapped.id}/ledger_accounts/${mapped.ledgerAccountId}`;
    req.throwOn(403, ErrorMessageError, 'forbidden');
    req.throwOn(404, ErrorMessageError, 'not found');
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.call(requestOptions);
  }

  /**
   * Add a ledger account category to a ledger account category.
   *
   * @param id              id
   * @param subCategoryId   sub_category_id
   * @return Response from the API call
   */
  async addLedgerAccountCategoryToLedgerAccountCategory(
    id: string,
    subCategoryId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      id: [id, string()],
      subCategoryId: [subCategoryId, string()],
    });
    req.appendTemplatePath`/api/ledger_account_categories/${mapped.id}/ledger_account_categories/${mapped.subCategoryId}`;
    req.throwOn(403, ErrorMessageError, 'forbidden');
    req.throwOn(404, ErrorMessageError, 'not found');
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.call(requestOptions);
  }

  /**
   * Delete a ledger account category from a ledger account category.
   *
   * @param id              id
   * @param subCategoryId   sub_category_id
   * @return Response from the API call
   */
  async deleteLedgerAccountCategoryFromLedgerAccountCategory(
    id: string,
    subCategoryId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({
      id: [id, string()],
      subCategoryId: [subCategoryId, string()],
    });
    req.appendTemplatePath`/api/ledger_account_categories/${mapped.id}/ledger_account_categories/${mapped.subCategoryId}`;
    req.throwOn(403, ErrorMessageError, 'forbidden');
    req.throwOn(404, ErrorMessageError, 'not found');
    req.throwOn(422, ErrorMessageError, 'unsuccessful');
    req.authenticate([{ basicAuth: true }]);
    return req.call(requestOptions);
  }
}
