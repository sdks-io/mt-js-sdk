/**
 * Modern TreasuryLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { AccountCollectionFlowController } from '../src';
import { testClient } from './testClient.js';
import { expectHeadersToMatch, makeApiCall } from './testHelper.js';

describe('AccountCollectionFlowController', () => {
  let controller : AccountCollectionFlowController;

  beforeAll(() => {
    controller = new AccountCollectionFlowController(testClient);
  });

  it('should Test listAccountCollectionFlows', async () => {
    const response = await makeApiCall(
      () => controller.listAccountCollectionFlows(
        undefined,
        undefined,
        undefined,
        undefined,
        undefined,
        undefined
      )
    );

    expect(response.statusCode).toBe(200);

    const expectedHeaders = {
      'X-After-Cursor': ['', false],
      'X-Per-Page': ['', false],
      'Content-Type': ['application/json', true],
    };

    expectHeadersToMatch(response.headers, expectedHeaders);
  });

  it('should Test createAccountCollectionFlow', async () => {
    const response = await makeApiCall(
      () => controller.createAccountCollectionFlow(undefined, undefined)
    );

    expect(response.statusCode).toBe(201);

    const expectedHeaders = { 'Content-Type': ['application/json', true] };

    expectHeadersToMatch(response.headers, expectedHeaders);
  });
});
